   class Point {
       constructor(x, y) {
           this._x = x;
           this._y = y;
       }
   }

   const point = new Point(5, 10);
   console.log(point._x); // Outputs: 5
   console.log(point._y); // Outputs: 10
   